<svg class="svg-inline--fa {{classes}}"
    width="{{width}}"
    height="{{width}}"
    role="img"
    viewBox="{{fontawesomeViewData icon set}}">
    <path fill="currentColor" d="{{fontawesomePathData icon set}}"></path>
</svg>